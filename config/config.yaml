# Directories
directories:
  raw: "data/raw"
  reference: "data/reference"
  qc: "results/qc"
  align: "results/align"

# Parameters
params:
  download-threads: 4
  qc-threads: 4          # fastqc only; multiqc is single-threaded
  bwa-threads: 8         # bwa mem scales ~linearly to ~12 on this CPU
  sort-threads: 4        # bwa + sort run concurrently = 12 total
  sort-mem-mb: 1024      # per sort thread, keeps sort within mem_mb
